#include <stdio.h>

int main() {
    // Laço for que percorre os números de 1 a 100
    for (int i = 1; i <= 100; i++) {
        printf("%d\n", i);  // Imprime o valor de i seguido de uma nova linha
    }

    return 0;  // Retorna 0, indicando que o programa terminou com sucesso
}
